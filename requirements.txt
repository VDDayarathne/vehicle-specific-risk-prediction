# ============================================================
# requirements.txt — KaduGuard Project Dependencies
# Install:  pip install -r requirements.txt
# ============================================================

# ── ML / Data Science ─────────────────────────────────────────
scikit-learn>=1.6,<2
xgboost>=2.1,<3
imbalanced-learn>=0.13,<0.15
shap>=0.46,<1
numpy>=2.1,<3
pandas>=2.2,<3
openpyxl==3.1.2           # Excel file support

# ── Data collection ───────────────────────────────────────────
requests==2.31.0
httpx>=0.27,<1             # Async HTTP client (used by backend)
osmnx>=1.9,<2              # OpenStreetMap road geometry

# ── Backend (FastAPI) ─────────────────────────────────────────
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.10,<3
python-multipart==0.0.9    # Form data parsing
python-dotenv>=1.0,<2      # Load .env files
# ── Database (SQLAlchemy ORM + PostgreSQL) ────────────────────────────────────
sqlalchemy>=2.0,<3          # ORM for database
psycopg2-binary>=2.9,<3     # PostgreSQL adapter
alembic>=1.13,<2            # Database migrations
pydantic-settings>=2.0,<3   # Environment variable management

# ── Authentication ────────────────────────────────────────────────────────────
PyJWT>=2.8,<3               # JWT token generation & validation
passlib[bcrypt]>=1.7,<2     # Password hashing
python-jose[cryptography]>=3.3,<4  # Token signing
bcrypt>=4.1,<5              # Bcrypt for password hashing

# ── Notifications ────────────────────────────────────────────────────────────
firebase-admin>=6.5,<7      # Firebase Cloud Messaging / notifications
# ── Testing ───────────────────────────────────────────────────
pytest>=8.0,<9
httpx>=0.27,<1             # AsyncClient for FastAPI TestClient

# ── Visualisation (notebooks / outputs) ───────────────────────
matplotlib>=3.9,<4
seaborn>=0.13,<1
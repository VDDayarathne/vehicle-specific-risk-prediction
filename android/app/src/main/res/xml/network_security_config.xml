<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow HTTP during local development so the app can target emulator, USB device, or LAN hosts. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
